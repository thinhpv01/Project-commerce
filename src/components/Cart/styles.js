import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    toolbar: {
        marginTop: '5.5rem',
    },
    title: {
        marginBottom: '1rem',
    },
    cardDetails: {
        width: '100%',
        display: 'flex',
        justifyContent: 'space-between',
        margin: '3rem 0 2rem',
    },
}));
